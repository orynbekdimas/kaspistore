services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      # міндетті: өз кілтіңді қой
      - GEMINI_API_KEY=${AIzaSyAtYyQHOIWzvrjyLXjv_SU2gEn8hkZMDCc}
      # қалауыңа қарай
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-3-flash-preview}
      - SECRET_KEY=${SECRET_KEY:-dev-secret-password}
      - DATABASE_URL=sqlite:////data/app.db
    volumes:
      # SQLite базасын контейнер өшсе де сақтап тұрады
      - ./data:/data
